# Packet C1 — Native Codex subagents alongside Claude Code

Authorized by Aaron on 2026-09-04 ("nothing we change here will affect claudes ability
to work. this is purely an add on for codex to mimic function"). Base: `main` at
`89b92535603d1397bbec90f9be00a1bcd9fdd4bc`. Branch:
`claude/native-codex-subagents`. Governing: `AGENTS.md`, `plan.md` sections 5–7 and
Phase 1 item 2, `docs/AGENT_TEAM.md`, `docs/CODEX_NOTES.md`,
`docs/decisions/0002-owner-goals-asked-properly.md`, and official OpenAI model guidance.
Line numbers below were read by the lead on 2026-09-04 against the base; verify each
before editing — **if the code disagrees with this packet, the code is the fact**.

The ask-first rule **does apply and is answered**: Aaron approved a Codex-only add-on.
The Claude role files and their Opus/Sonnet routing must retain their behavior. New
downstream `.codex/agents/*.toml` files are treated as ask-first because they instruct
future projects.

Do not replace the shared `CLAUDE.md`/`AGENTS.md` rule source, duplicate packets under
`.codex/`, or change Claude role behavior.

## What the lead measured

- Four native Codex files exist only as untracked root files — `git status --short`,
  2026-09-04: `?? .codex/`; no `templates/.codex/agents/` files or git history exist.
- The four files are mechanical substitutions of `.claude/agents/*.md` bodies. They
  corrupt `CLAUDE.md == AGENTS.md` into `AGENTS.md == AGENTS.md`, point ask-first at
  nonexistent paths, and invent `Codex/<slug>` branches.
- `INSTALL_MAP` and `AGENT_FILES` install/audit only Claude roles —
  `tools/jumpstart.py:34-54,76-81,812-823`.
- Codex docs still claim native subagents do not exist and require pasted Claude briefs
  — `docs/CODEX_NOTES.md:22-46`, `templates/codex/CODEX_NOTES.md:23-46`.
- The current Codex harness exposes Sol, Terra, and Luna. Official OpenAI guidance calls
  Terra the balanced capability/cost tier and Luna the cost-sensitive tier. Aaron chose
  Terra/high for tester, builder, and reviewer; Luna/medium for recon.

## Items

### 1. Ship native Codex role definitions (`templates/.codex/agents/*.toml`)

Add `tester.toml`, `builder.toml`, `reviewer.toml`, and `recon.toml`, plus filled dogfood
copies under root `.codex/agents/`. Preserve each Claude role's scope, handoff, shared
`claude/<slug>` branch prefix, packet path, ask-first paths, and CLAUDE/AGENTS parity
rule. Use `model = "gpt-5.6-terra"` with high effort for tester/builder/reviewer and
`model = "gpt-5.6-luna"` with medium effort for recon in this repo. Generic templates
use `{{CODEX_STRONG_MODEL}}` and `{{CODEX_CHEAP_MODEL}}` so future projects choose current
models rather than inheriting a stale fixed name.

Binding invariants: Codex is additive; Claude roles keep Opus/Sonnet and their behavior;
one shared rules source; one shared packet format; no project-specific template text.

Tests must prove: all four Codex templates exist and parse as TOML; strong/cheap model
routing is correct after fill; semantic safety clauses use the real branch prefix,
ask-first paths, and CLAUDE/AGENTS equality; Claude role model routing is unchanged.
At least one install/model assertion must fail on the base because no Codex templates
are installed there.

### 2. Install and audit both harness-native role sets

Extend `INSTALL_MAP` and the role audit so `init` writes and `retrofit` checks all four
Codex roles without ambiguous duplicate finding names. Extend the gitignore contract so
`.codex/agents/` is tracked without treating other Codex project configuration as
machine-local. Preserve `init` no-overwrite and `retrofit` report-only behavior.

Tests must prove: `init` writes all Codex roles; supplied Codex model substitutions are
filled while unsupplied ones stay visible and make `check` fail; retrofit reports a
missing Codex role and a fully initialized repo stays clean; gitignore tracking is
correct and idempotent. Existing Claude init/audit tests remain untouched and green.

### 3. Replace the obsolete manual Codex handoff documentation

Update filled and template `CODEX_NOTES.md`, both `AGENT_TEAM.md` files, README, and the
new-project/retrofit/build-loop playbooks to describe native Codex spawning, model tiers,
and the same shared packet/handoff interface. Amend the shared `CLAUDE.md`, then run
`sync-agents`, only as needed to say each harness uses its native role directory and to
add new Codex role templates to ask-first. Do not change `.claude/agents/*.md`.

Add an `INTERNALS.md` incident explaining why mechanical role conversion is unsafe:
the observed equality, ask-first path, and branch-prefix corruptions. Mirror the generic
rule in the template internals where required.

## Docs to reconcile (same branch)

- `CURRENT_CHECKPOINT.md` — make C1 active, record verification, and keep gate 4 open
  until a real Codex crossing completes.
- `CHANGELOG.md` — inventory native Codex roles and add one recent-change entry.
- `plan.md` — narrow Phase 1 item 2: native machinery green, crossing still owed.
- `docs/INTERNALS.md` — record the failed mechanical conversion and its rule.
- `docs/README.md` and `templates/docs/README.md` — classify native Codex definitions.
- `CLAUDE.md` + `AGENTS.md` — remain byte-identical after `sync-agents`.

## Gates before handoff

Probe that no test-selection condition is active; `python -m pytest tests/ -q` exit 0;
`ruff check .` clean; `python tools/jumpstart.py check .` exit 0; `python
tools/jumpstart.py retrofit .` exit 0. Report process exit codes. No build or packaging
trigger exists.

## The real-world gate

**A Codex lead spawns at least one native Codex role from the tracked TOML, hands it the
same packet path used by Claude Code, and records that the handoff/verdict survives the
crossing.** Gate 4 stays open until observed.

## Still owed after C1

Phase 1 gate 2 remains: bootstrap one genuinely new project end to end and record which
questions and placeholders were hard to answer.
