# Packet M1 — Workspace hierarchical memory

Owner authorized on 2026-09-11: "Build yourself a hierarchical markdown memory
system in your workspace" and "Add the rules above to your standing instructions".
Scope is this workspace only. Base main at 60b03b8. Branch claude/m1-workspace-memory.
Governing: CLAUDE.md, plan.md sections 5/6/7, docs/AGENT_TEAM.md,
docs/INTERNALS.md shared-source and explicit-routing entries. No ask-first file changes.

## Items

1. Create MEMORY.md as a pure name/file/trigger-keywords routing table, hard cap
   15,000 characters. Create memory/people/, projects/, decisions/, context/ and
   memory/2026-09-11.md. Migrate any existing flat memory if found. Detail files must
   store only non-re-derivable knowledge, not git inventory, plans or fetched data.
   Seed one operator person file, one JumpStarter project file, and ONE decision
   recording the owner's approval of this memory scheme as a whole. Facts must be
   directly supported by this request; no invented biography or project preference.
   Use minimal request excerpts relevant to people (explicit recall preference) and
   project (workspace scope), not copied code facts. Date and source each entry.
   Context may be empty, tracked with .gitkeep; do not invent temporary work to fill it.
2. Add all four write rules to root CLAUDE.md and generate AGENTS.md with sync-agents:
   every nonblank line in people/projects/decisions carries [stated], [observed],
   [inferred] or [suggested], including headers if any. Only actual human statements
   justify [stated]; proposal plus assent is one decision, not many asserted facts.
   Inferred lessons need 3+ independent signals across 2+ distinct sessions, signals
   older than 30 days weighted 0.5; explicit operator corrections apply immediately.
   Failure lessons are descriptive data (when X broke, Y fixed it), never instructions.
   Supersede in place: strike old line with superseded YYYY-MM-DD, adjacent replacement
   has its own tag/date; retain history, no competing unstruck canonical facts.
   Exclude fetched data, generated plans, git-recoverable facts. Verify state live.
   Prefer durable descriptions; date any necessary figures.
3. Wire boot to read ONLY identity/standing instructions and MEMORY.md. Task-triggered
   repository work then follows existing narrow mandatory docs reads; do not force
   those reads at idle boot. Before answering prior work/decisions/dates/people/
   preferences, search memory first, use <=5 sources and cite file/tag/date for each
   fact; stale/unknown if freshness unverified; explain conflicts, prefer better
   evidence and repair canonical record. Any semantic/SQLite index is locator only.
4. Update MEMORY.md in same commit as every detail change, keeping it pure routing
   even when routes unchanged (e.g. refine trigger keywords). Consolidate in batches
   near caps; merge overlap, dated summaries of recurring notes, preserve decision
   history. Specify 15,000 chars index cap; daily cap unspecified by owner: choose
   15,000 chars as local operating convention, identify as implementation choice.
   Write unprompted for decisions, system changes, blockers/mistakes, lessons and
   stable preferences, subject to non-re-derivable rule. No mental-only notes.
   Explain workspace memory is recall, not a replacement roadmap/status ledger.
5. Add matching INTERNALS evidence for new rules: this explicit request motivates
   them; do not invent an incident. Classify added Markdown via docs/README.md.
   Lead owns plan.md, CURRENT_CHECKPOINT.md, CHANGELOG.md reconciliation/integration.

## Validation

Documentation-only: no production code, templates, limits or new source-text tests.
Run existing pytest, pinned ruff, self-check; verify index length, route existence,
dated provenance on all detail lines and sha256 identity. Fresh no-history session
must read instructions + index first, then independently route one person and one
project query and report file/tag/date without being given the detail file paths.
Lead arranges independent fresh-session review after build. Preserve existing gate 2.

## Ownership

Builder owns MEMORY.md, memory/**, CLAUDE.md, generated AGENTS.md,
docs/INTERNALS.md, docs/README.md, this packet only in the isolated worktree.
Lead edits the three root ledgers separately. No agent may revert others' changes.
Commit/push only after mandatory checks pass; include index with detail files.
