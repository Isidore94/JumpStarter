# Packet C2 — Astra lead and native Codex team parity

Authorized by Aaron on 2026-09-09: "ensure that the repo is setup such that codex
models mimic what the claude models do. for example Astra should be the orchestrator,
terra and luna sub agents should be used same as sonnet and opus."
Base: main at d3c3f81. Branch: claude/c2-astra-orchestration.
Governing: AGENTS.md, plan.md sections 5/6/7 and Phase 1 item 2,
docs/AGENT_TEAM.md, docs/INTERNALS.md native-role and shared-source entries.
The file-scoped ask-first rule applies to templates; the request above authorizes
these model-routing and parity changes. No further approval is owed for this scope.

## Verified premises

- .codex/agents/recon.toml:1 and builder.toml:1 already select Luna/medium and
  Terra/high respectively; tester/reviewer also select Terra/high.
- All eight root/template Codex role TOMLs omit name and description.
- No .codex/config.toml is tracked. docs/CODEX_NOTES.md:26 keeps the session model.
- tools/jumpstart.py:34 INSTALL_MAP installs four role TOMLs, not a lead config;
  build_parser at line 1055 accepts only strong/cheap model flags.
- Official schema requires name, description, developer_instructions in native roles:
  https://learn.chatgpt.com/docs/agent-configuration/subagents

## Items

1. Supply name and useful role-specific description in all eight Codex role TOMLs.
   Preserve role bodies, scope, handoffs, and existing Terra/high versus Luna/medium
   routing. Do not change Claude role definitions.
2. Track .codex/config.toml with model gpt-6-astra, high reasoning, agents.enabled
   true and fallback subagent model gpt-5.6-terra/high. Generic template uses
   CODEX_LEAD_MODEL and CODEX_STRONG_MODEL placeholders; add --codex-lead-model to
   init and install this config through normal non-overwriting INSTALL_MAP behavior.
   No permission/sandbox changes. Missing models remain visible placeholders.
3. Audit missing native lead configuration and missing required role metadata on
   retrofit and check. Keep Python 3.9 stdlib compatibility. Audit the supported
   native metadata contract conservatively; do not invent a full TOML parser.
   Do not enforce JumpStarter's particular model IDs on downstream project choices.
4. Make lead delegation explicit in root/template CLAUDE.md (then sync AGENTS.md),
   team docs and Codex notes: Astra leads here; Terra tester/builder/reviewer match
   Claude Opus responsibilities; Luna recon matches Sonnet. Recon then independent
   tester, builder, reviewer with shared packets, worktree isolation and handoffs.
   If the runtime lacks a native-role selector, disclose the limitation once, read
   the tracked role TOML and explicitly pass its model, effort, developer instructions,
   packet and isolated worktree path to the generic spawn tool with fork_turns="none".
   Call this an adapted role run; it does not close the native-role gate. If spawning
   itself is unavailable, report the blocker. Never silently use inherited Astra workers.
   Project config defaults
   cannot retroactively switch an existing session or override explicit UI choices.
   Templates retain configurable model choices. Mirror affected playbooks and README.

## Validation and handoff

Tester writes test_c2_* behavior tests first, proves red, and hands to builder.
Exercise real init (configured and omitted lead model), no-overwrite/force, check and
report-only retrofit against missing config/metadata; validate all role fields.
Existing C1 tests remain intact. Run full pytest, ruff 0.16.6, check and retrofit.
Reconcile checkpoint active block, changelog inventory/recent entry, plan item 2,
INTERNALS rule evidence and docs index as necessary. Keep gate 2 open; gate 4 only
closes if a real native role spawn is observed, not from schema tests or manually
reading a TOML into a generic subagent. No unrelated I2 work.
Reviewer reproduces independently. Lead integrates after GO. No running service.

## Lead runtime observations (2026-09-10)

On base d3c3f81, CPython 3.9.25: 57 tests passed, process exit 0; ruff 0.16.6 clean.
Codex CLI 0.153.4, `exec --ephemeral --strict-config`, actually reported all four
native roles ignored because `name` is absent. Both its default tool schema and a
probe with `--enable multi_agent_v2` lack a native-role selector; `task_name` is only
a task label. Current collaboration tools therefore exercise adapted roles, not a
native-role crossing on the baseline.

After the metadata fix, a fresh session exposed all four native roles and `agent_type`
without any feature flag. A live Astra session then spawned native `recon` with this
packet and branch, `fork_turns="none"`, no model/effort override, and received its
file/line handoff. CLI exit 0; run ID `01a08b7c-7d94-7ad0-b92c-deb08dd4fa65`.
The missing selector was a consequence of malformed role files. Gate 4 is closed.

## Handoff

63 tests pass on CPython 3.9.25; ruff 0.16.6 clean; check 12 findings/no gaps;
retrofit 30 findings/no gaps/one pre-existing advisory. All process exits 0.
The independent reviewer reproduced audit defects and the final regression suite covers
the fixes. Account usage limits interrupted final reviewer sign-off; the lead finished
documentation, the missing-file fix and stale inventory-count expectations. Final GO
is still required before integration. All Claude agent definitions remain unchanged.
